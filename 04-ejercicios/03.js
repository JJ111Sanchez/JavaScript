/* 
* Indice validar que no sea menor a cero y que el elemento exista en el array
*/

/* function getbyIdx(arr, idx) {
    if(idx < 0 || arr.length <= idx) {
        return 'Elemento no existe';
    }
    return arr[idx];
}

let resultado = getbyIdx([1, 2], 1);
console.log(resultado);
 */

//////////////////////////////////

/* function getByIdx(arr, idx){
    if(idx < 0 || arr.length <= idx){
        return 'Elemento no existe';
    }
    return arr[idx];
}

let resultado = getByIdx([1, 2, 3, 4, 5, 6], 5);
console.log(resultado); */

////////////////////////////////

/* function getByIdx(arr, idx){
    if(idx < 0 || arr.length <= idx){
        return 'Elemento no existe';
    }
    return arr[idx];
}

let resultado = getByIdx([5, 3, 9, 1], 2);
console.log(resultado); */

/* function getByIdx(arr, idx){
    if(idx < 0 || arr.length <= idx){
        return 'Elemento no existe';
    }
    return arr[idx];
}

let resultado = getByIdx([5, 78, 5, 2], 1);
console.log(resultado); */

/* function getByIdx(arr, idx){
    if(idx < 0 || arr.length <= idx){
        return 'Elemento no existe';
    }
    return arr[idx];
}

let resultado = getByIdx([4, 5, 8 ,52], 0);
console.log(resultado); */

/* function getByIdx(arr, idx){
    if(idx < 0 || arr <= idx){
        return 'Elemento menor que cero no existe';
    }
    return arr[idx];
}

let resultado = getByIdx([1, 2, 3, 4, 5], 3);
console.log(resultado); */

/* function getByIdx(arr, idx){
    if(idx < 0 || arr <= idx){
        return 'Elemento menor a cero no existe';
    }else if(idx > 0 || arr <= idx){
        return 'Elemento ingresado no existe';
    }
    return arr[idx];
}

let resultado = getByIdx([5, 2, 3, 4, 5,], 8);
console.log(resultado); */

